@model FBE.ViewModels.Kosul.KosullarIndex
@{
    ViewData["Title"] = "Index";
}

<h1>Başvuru Koşulları</h1>
<form method="post">
    <div class="row">
        <div class="col-12">
            <div class="float-right">
                <a asp-area="admin" asp-controller="Conditions" asp-action="Create">
                    <i class="fa fa-plus"></i>
                    <span class="hidden-xs-down">Yeni Basvuru Koşulu</span>
                </a>
            </div>
        </div>

    </div>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-area="admin" asp-controller="Home" asp-action="Index">Ana Sayfa</a></li>
            <li class="breadcrumb-item active" aria-current="page">Basvuru Koşulu</li>
        </ol>
    </nav>


    <div class="tab-pane fade show active" id="nav-tr" role="tabpanel" aria-labelledby="nav-tr-tab">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Program Adı</th>
                    <th scope="col">Öğretim Yılı</th>
                    <th scope="col">Dönem</th>
                    <th scope="col">Sil/Düzenle</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Tr)
                {
                    <tr>
                        <td>@item.Program.Prog_Ad</td>
                        <td>@item.Ogretim_Yili</td>
                        <td>@item.Donem</td>
                        <td>
                            <a class="btn btn-danger" asp-area="admin" asp-controller="Conditions" asp-action="Delete" asp-route-id="@item.Basvuru_Kos_Tr_Id">Sil</a>
                            <a class="btn btn-primary" asp-area="admin" asp-controller="Conditions" asp-action="Edit" asp-route-id="@item.Basvuru_Kos_Tr_Id">Düzenle</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</form>

